{
  "buildCommand": "npx ng build --configuration production",
  "outputDirectory": "dist/devbootcamp/browser",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
